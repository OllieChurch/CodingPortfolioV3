[build]
  command = "npm run build"
  publish = "dist"

# Force all URLs to lowercase so routes are never case-sensitive
[[redirects]]
  from = "/blog/every-service-I-build"
  to = "/blog/every-service-i-build"
  status = 301
  force = true
